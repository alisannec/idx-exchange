# IDX Exchange Project

The objective is to develop an ML model to predict the close price (the final transaction price of a property) of any single residential property in California.

## Group

ds52

## Installation

First use pip to install pandas.

```bash
%pip install pandas
```

Install matplotlib to view visualizations.

```bash
%pip install matplotlib
```